```
create table takeleave.leavetake(id int, name varchar(30), `time-from` date, `time-end` date, primary key (id)); creat table
```

```
insert takeleave.leavetake(id, name, `time-from`, `time-end`) values(1, 'rondou.chen', '2016-09-09', '2016-09-10'); insert data
```

```
drop tables leavetake; delete table
```

```
update takeleave.leavetake SET `time-from`='1984-01-01' WHERE `id`='1'; update data base tables
```

```
show tables; show all tables
```

```
show databases show all database
```

```
mysql -u rondou --password=PASSWORD ;login mysql
```
